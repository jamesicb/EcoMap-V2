{
  "name": "ecomap-v2",
  "private": true,
  "scripts": {
    "dev": "node node_modules/serve/build/main.js . -l 5173",
    "dev:edge": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"$p = Get-NetTCPConnection -LocalPort 5173 -State Listen -ErrorAction SilentlyContinue | Select-Object -First 1 -ExpandProperty OwningProcess; if ($p) { Stop-Process -Id $p -Force }; Start-Process msedge 'http://localhost:5173'; serve . -l 5173\"",
    "test": "node --test tests/privacy-utils.test.mjs"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
